{
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(?!assets|api)([^.]+)",
      "destination": "/api/page?guest=$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
